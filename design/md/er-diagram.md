# Legacy location

The canonical current Mermaid ER diagram is [../er-diagram.md](../er-diagram.md). The existing PNG under `design/png/` was retained because a local Mermaid renderer was unavailable.
