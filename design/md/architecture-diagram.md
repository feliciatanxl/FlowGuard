# Legacy location

The canonical current Mermaid architecture diagram is [../architecture-diagram.md](../architecture-diagram.md). The existing PNG under `design/png/` was retained because a local Mermaid renderer was unavailable.
