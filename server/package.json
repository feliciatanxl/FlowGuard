{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "dev": "nodemon index.js",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.16.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "form-data": "^4.0.5",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "node-cron": "^4.6.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.20.0",
    "pg-hstore": "^2.3.4",
    "pgvector": "^0.2.1",
    "sequelize": "^6.37.8",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "jest": "^30.4.2",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  },
  "//overrides": "uuid<11.1.1 (GHSA-w5hq-g745-h8pq) reaches us only transitively via sequelize@6.37.8, which calls uuid.v1()/v4() with no `buf` argument — the vulnerable buffer-bounds path is never exercised. sequelize has no patched 6.x release that bumps uuid, and `npm audit fix --force` would downgrade sequelize to 3.x (breaking). brace-expansion/js-yaml are DEV-ONLY jest transitives (never in the deployed server) pinned to the smallest API-compatible patched version; js-yaml stays on v3 for @istanbuljs/load-nyc-config. `npm audit fix` was rejected because it destabilised the toolchain.",
  "overrides": {
    "uuid": "^11.1.1",
    "brace-expansion": "5.0.8",
    "js-yaml": "3.15.0"
  }
}
